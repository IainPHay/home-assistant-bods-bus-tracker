# v0.6 validation record

This document is the durable validation record for the BODS Bus Tracker v0.6 development series. It records automated validation, real Home Assistant testing, bugs found during beta testing, fixes applied, and the remaining criteria for stable v0.6.0.

## Scope of v0.6

v0.6 combines four substantial changes:

1. provider-neutral routed dynamic walking time;
2. shared/rate-limited BODS live-feed acquisition;
3. shared and persistent GTFS startup/index optimisation;
4. Home Assistant quality-scale hardening, including native tests, Repairs and strict typing.

The live-to-GTFS ETA matching policy remains deliberately conservative and is not rewritten by the dynamic walking work.

## Beta.1 — UI architecture

Validated in real Home Assistant:

- the integration is declared `single_config_entry`;
- the redundant **Add hub** path is removed;
- **Add bus stop** remains available;
- existing multi-stop/subentry behaviour remains intact.

## Beta.2 — shared BODS live feed and auth handling

A real-world false reauthentication event exposed excessive request volume from the old architecture and from the retained standalone Fairway MQTT tracker.

Changes validated:

- one shared account-level BODS client;
- operator-filtered SIRI-VM feed reused across configured stops;
- route filtering is local;
- successful and failed operator responses cached for 15 seconds;
- concurrent same-operator requests de-duplicated;
- real upstream request starts serialised with at least 6 seconds between starts;
- only HTTP 401 triggers reauthentication;
- HTTP 403 is `access_forbidden`;
- HTTP 429 is `rate_limited`;
- timeout/connection errors fall back without invalidating credentials;
- the native numeric delay sensor is unavailable when no live delay exists instead of exposing a non-numeric state.

The legacy standalone `fairway-bus.service` was disabled before validation so it could not continue generating four route-level BODS requests per cycle.

Real diagnostics confirmed the shared feed returned the configured Fairway/Haymarket services, live matching was healthy, and the existing API key worked without a false reauth flow.

## Beta.3 — GTFS startup optimisation

Beta.2 diagnostics measured approximately 250.5 seconds for config-entry setup because Fairway and Haymarket independently parsed the same North East regional `stop_times.txt`.

Beta.3 changed this to:

- one shared per-region parsed GTFS index;
- union of configured services per region;
- lightweight per-stop filtering from the shared index;
- persistent JSON parsed-index cache;
- strict cache key based on GTFS fingerprint, service date, exact service set and cache schema;
- no pickle or executable cache format.

Real Home Assistant validation:

- first beta.3 restart rebuilt one shared North East index and populated both stops;
- first rebuild setup was approximately 106.6 seconds, down from the ~250.5 second beta.2 baseline;
- a subsequent same-day restart restored the parsed index from disk in approximately 0.964 seconds;
- later beta.4 diagnostics showed gross config-entry setup around 42.1 seconds, of which about 39.7 seconds was Home Assistant platform-import waiting; actual BODS integration work was about 2.4 seconds;
- Fairway restored the shared index from disk in about 0.137 seconds and Haymarket reused it from memory;
- live BODS/GTFS matching remained healthy.

Automated cache-policy tests prove that GTFS fingerprint, service date, service set and schema changes invalidate the persistent parsed cache.

## Beta.4 — Home Assistant quality hardening

Implemented:

- `PARALLEL_UPDATES = 0` for coordinator-managed entity platforms;
- explicit live-feed task cancellation and runtime-cache cleanup on unload;
- persistent GTFS/index cache removal when the integration is deleted;
- one-time live-data degraded/outage/recovery logging;
- translated config errors, entity names/states and integration exceptions;
- `icons.json` entity icon translations;
- suitable duration/timestamp/enum device classes;
- lower-value diagnostic entities disabled by default for new registrations;
- bus stops represented as logical Home Assistant service devices;
- stale stop-device removal support;
- self-clearing Repair for a genuinely missing configured routed-walking sensor;
- removal/data-update/use-case/automation documentation;
- generic Leave-now automation blueprint.

### Automated quality validation

The v0.6 beta series now has:

- Home Assistant-native tests using `pytest-homeassistant-custom-component`;
- 95% minimum coverage enforced in CI;
- configuration-flow coverage at 100%;
- strict Home Assistant-style mypy validation;
- HACS validation;
- Hassfest validation;
- runtime/manifest version-sync validation;
- GTFS cache-policy validation.

The beta.4 release candidate measured 96% overall integration coverage.

### Real beta.4 Home Assistant validation

Passed:

- both The Fairway and Haymarket loaded under v0.6.0-beta.4;
- translated timing/data states rendered correctly;
- service-device presentation rendered correctly;
- no BODS warnings/errors after restart;
- both coordinators reported healthy live feed/matching;
- existing native BODS registry entities were retained without duplicate `_2`/parallel entities;
- unload/reload repopulated entities cleanly with no BODS log errors;
- live/timetable/terminus behaviour remained intact.

A missing routed-walking entity correctly produced:

- `walking_mode: static_fallback`;
- `walking_dynamic_minutes: null`;
- `walking_fallback: true`;
- `walking_source_status: missing`;
- continued use of the configured 5-minute static walking fallback.

The translated Home Assistant Repair **Dynamic walking-time sensor is missing** appeared and was attributed to BODS Bus Tracker. Disabling routed dynamic walking removed the Repair automatically.

## Beta.5 — reconfigure-flow performance

During the beta.4 Repair test, opening an existing stop's **Reconfigure** flow took long enough to fail.

Root cause:

- `discover_stop_services()` rescanned the regional `stop_times.txt`;
- it CSV-parsed every row even though reconfiguration only needed rows for one stop;
- this bypassed the startup optimisation and could exceed Home Assistant's flow timing tolerance.

Beta.5 optimisation:

- raw UTF-8 `stop_times.txt` lines are first cheaply filtered for the target stop token;
- only candidate lines are CSV-parsed;
- exact `stop_id` column matching is still required;
- quoted stop IDs remain supported;
- false positives where the stop text appears in another GTFS column are rejected;
- all services calling at the stop remain selectable.

Automated beta.5 validation:

- 146 tests;
- 96% overall integration coverage;
- HACS validation passed;
- Hassfest passed;
- version-sync passed;
- GTFS cache-policy passed;
- strict mypy passed.

Real Home Assistant validation:

- v0.6.0-beta.5 installed successfully;
- The Fairway reconfigure form opened in approximately **7 seconds**;
- the previous timeout no longer occurred;
- missing walking sensor still fell back to static walking time;
- the Repair appeared correctly;
- disabling routed walking self-cleared the Repair.

## Routed dynamic walking — behaviour already proven

Proven by automated and live testing:

- feature is opt-in per stop;
- static walking remains the fallback;
- missing configured source falls back safely;
- transient invalid source states do not crash the stop;
- seconds/minutes/hours are normalised;
- routed values are rounded up to a whole minute for leave guidance;
- routed values above the per-stop configured maximum are rejected; the backward-compatible default is 120 minutes;
- stale values fall back;
- arrival-only mode does not expose leave guidance;
- routing logic is downstream of ETA/matching;
- BODS Bus Tracker does not store routing-provider credentials;
- BODS Bus Tracker does not force-update the provider on every BODS poll;
- no person/device coordinates are copied into BODS walking diagnostics.

## Beta.6 — configurable routed-walking ceiling

Live HERE testing showed that a hard-coded 120-minute cutoff is useful as a conservative default but should not be a universal policy. Beta.6 therefore makes the threshold configurable per stop.

Design:

- existing stops default to 120 minutes without migration;
- add/reconfigure flows expose **Maximum routed walking time**;
- accepted range is 1–1440 minutes;
- the configured value is applied only to the dynamic provider duration;
- static fallback behaviour is unchanged;
- `walking_max_dynamic_minutes` is exposed in Next bus attributes;
- automated coverage proves a ~407-minute duration remains invalid at the default 120-minute limit but becomes valid if the stop is configured with a 500-minute limit.

Real Home Assistant beta.6 validation on 2026-09-11:

- beta.6 installed successfully;
- Haymarket reconfigure exposed **Maximum routed walking time** and preserved the backward-compatible **120 min** default for the existing stop;
- routed walking remained enabled and retained `sensor.here_travel_time_duration`;
- changing only the maximum from 120 to **500 minutes** caused the real HERE duration of `407.266666666667` minutes to be accepted;
- runtime reported `walking_minutes: 408`, `walking_mode: dynamic`, `walking_dynamic_minutes: 407.266666666667`, `walking_max_dynamic_minutes: 500`, `walking_fallback: false`, and `walking_source_status: ok`;
- for the next 22:59 departure, the 408-minute effective walk produced `leave_by: 16:11`, `leave_in_minutes: 0`, and `leave_now: true`, which is internally consistent because the required leave time was already in the past;
- bus tracking remained healthy simultaneously with `data_status: ok`, 15 live vehicles and 4 GTFS matches, while the combined terminus view continued to expose scheduled departures and a live inbound X18 arrival.
- beta.6 unavailable-path recheck at 2026-09-11 22:57 BST with the maximum still set to 500 minutes also behaved correctly: `walking_minutes: 0`, `walking_mode: disabled`, `walking_dynamic_minutes: null`, `walking_max_dynamic_minutes: 500`, `walking_fallback: true`, and `walking_source_status: unavailable`; leave guidance cleared to null/false while the bus tracker remained healthy (`data_status: ok`, 16 live vehicles, 5 GTFS matches).
- at the same capture, a real X15 outbound journey was matched live at Haymarket with vehicle 7714, `at_stop: true`, `stop_role: origin`, and appeared in `terminus.at_stand_departures`; an inbound live X18 remained independently represented as an arrival. This is useful regression evidence that walking-provider failure does not interfere with terminus live matching.

## Remaining real-provider validation before stable v0.6.0

Use HERE Travel Time first.

Required live checks:

- [x] Configure a real HERE pedestrian route from the Home Assistant **Abbi person entity** to **Haymarket Bus Station Stand Q (ATCO 4100008HAYQS)**.
- [x] Select the HERE **Duration** sensor in the Haymarket stop reconfiguration.
- [x] Confirm `walking_mode: dynamic` using the real HERE Duration entity with its Home Assistant state temporarily overridden to 8.4 min.
  - Captured at 2026-09-11 20:12 BST: `walking_minutes: 9`, `walking_mode: dynamic`, `walking_time_entity: sensor.here_travel_time_duration`, `walking_dynamic_minutes: 8.4`, `walking_fallback: false`, `walking_source_status: ok`.
  - For the 20:29 Haymarket departure, `leave_by` became 20:20, `leave_in_minutes` was 8 at capture time, and `leave_now` remained false.
- [x] Confirm `walking_time_entity` points to `sensor.here_travel_time_duration`.
- [x] Confirm `walking_dynamic_minutes` contains the provider duration (8.4 min in the forced-state positive-path test).
- [x] Confirm `walking_fallback: false` and `walking_source_status: ok`.
- [ ] Confirm changing tracked location causes the routed duration to change.
  - Current real HERE pedestrian duration for Abbi → Haymarket is ~407.27 min while Abbi is far from Newcastle; this is intentionally above the then-configured 120-minute safety ceiling.
  - Baseline captured at 2026-09-11 20:10 BST with Haymarket configured to `sensor.here_travel_time_duration`: `walking_minutes: 0`, `walking_mode: disabled`, `walking_dynamic_minutes: null`, `walking_fallback: true`, `walking_source_status: invalid`, `leave_by: null`, `leave_in_minutes: null`, `leave_now: false`.
  - At the same time the Haymarket tracker remained healthy (`data_status: ok`, 18 live vehicles, 9 GTFS matches) and terminus arrivals/departures continued to populate.
  - Abbi is expected to be at/near Haymarket on the morning of 2026-09-12, providing the positive live transition test from excessive/invalid routed duration to a valid dynamic walking duration.
  - Beta.6 live ceiling test captured at 2026-09-11 22:50 BST: with the per-stop maximum changed from 120 to 500 minutes, the same real provider duration was accepted as dynamic: `walking_dynamic_minutes: 407.266666666667`, `walking_minutes: 408`, `walking_max_dynamic_minutes: 500`, `walking_fallback: false`, `walking_source_status: ok`.
- [x] Confirm **Leave by**, **Leave in** and **Leave now** follow the effective routed walking duration in the forced-state positive-path test.
- [x] Confirm temporary provider `unavailable` state falls back to the configured static time without a Repair.
  - Captured live at 2026-09-11 20:17 BST with Haymarket static fallback 0: `walking_minutes: 0`, `walking_mode: disabled`, `walking_dynamic_minutes: null`, `walking_fallback: true`, `walking_source_status: unavailable`, with all Leave guidance cleared.
  - Haymarket bus tracking remained healthy at the same time (`data_status: ok`, 18 live vehicles, 9 GTFS matches), and terminus approaching-arrival data continued to populate.
  - A transient provider state does not create the missing-entity Repair because the configured entity still exists.
- [x] Confirm provider recovery automatically returns to `walking_mode: dynamic`.
  - Captured live at 2026-09-11 23:00 BST without reconfiguring BODS after the beta.6 unavailable test: HERE recovered to its real ~407.27-minute value and Haymarket automatically returned to `walking_mode: dynamic`.
  - Recovery attributes: `walking_minutes: 408`, `walking_dynamic_minutes: 407.266666666667`, `walking_max_dynamic_minutes: 500`, `walking_fallback: false`, `walking_source_status: ok`.
  - Bus tracking remained healthy simultaneously (`data_status: ok`, 16 live vehicles, 5 GTFS matches). A real X15 outbound vehicle 7714 remained matched at stand while the inbound X18 remained separately represented as an arrival.
- [ ] Confirm no routing-provider credentials or coordinates appear in BODS diagnostics.
- [ ] Confirm ordinary Haymarket departure ETA/live matching remains unchanged while dynamic walking is active.
- [ ] Confirm Haymarket terminus arrival/departure behaviour remains unchanged.

## Stable v0.6.0 release checklist

Do not merge/release stable v0.6.0 until the real-provider checks above pass.

Then:

- [ ] change runtime and manifest versions to stable `0.6.0`;
- [ ] update the main README to describe final v0.6 behaviour rather than beta/future-work wording;
- [ ] refresh Home Assistant screenshots for the final multi-stop, terminus and routed-walking experience;
- [ ] verify every screenshot/link on the GitHub repository page;
- [ ] verify the README as rendered by HACS and update HACS-facing screenshots/text;
- [ ] update stable release/version badges and links;
- [ ] consolidate beta changelog wording into the stable v0.6.0 release notes;
- [ ] run HACS, Hassfest, version-sync, cache-policy, HA-native coverage and strict-typing validation on the exact stable commit;
- [ ] merge PR #7 only after final Home Assistant regression testing and documentation/screenshot review;
- [ ] publish stable v0.6.0;
- [ ] verify the stable tag points to the merged release commit;
- [ ] after stable confidence is established, remove the retained legacy Fairway MQTT tracker/discovery entities and rollback files.
